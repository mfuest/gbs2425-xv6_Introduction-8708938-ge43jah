#include "kernel/types.h"
#include "kernel/stat.h"
#include "kernel/fcntl.h"
#include "kernel/defs.h"
#include "user/user.h"

int main(void) {
    // Do magic ╰( ͡° ͜ʖ ͡° )つ──☆*:・ﾟ and implement your cp program here.

    // Note that the main method always has to call exit in the end, so 
    // the process actually terminates and frees its resources.

    exit(EXIT_FAILURE);
}
